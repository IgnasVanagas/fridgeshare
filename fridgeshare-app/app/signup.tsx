import mainStyle from '@/styles/styles';
import { View, Text } from 'react-native';

export default function SignupScreen() {
	return (
		<View style={mainStyle.container}>
			<Text style={mainStyle.text}>Registracija</Text>
		</View>
	);
}
